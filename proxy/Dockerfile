# Используем легковесный образ nginx на базе Alpine.
# Он очень маленький и содержит минимум компонентов, что хорошо для безопасности.
FROM nginx:1.23-alpine

# Удаляем стандартную конфигурацию Nginx, которая идет в комплекте с образом.
# Мы заменим ее своей.
RUN rm /etc/nginx/conf.d/default.conf

# Копируем наш собственный файл конфигурации в нужное место внутри контейнера.
COPY nginx.conf /etc/nginx/conf.d/default.conf